# Piscipedia Rank Predictor
## Run
pip install -r requirements.txt
Set SMTP environment variables from .env.example, then:
python app.py

The SQLite database stores new form responses and predictions update as responses grow.
Email delivery to oderexdc@gmail.com requires your own SMTP/App Password configuration.
